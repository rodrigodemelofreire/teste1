#Projeto lava rapido full stack (mysql, prisma, nodejs)
Para executar 
Clonar o projeto 
Abrir o console via cmd, instalar as dependencias via npm i (bcrypt, nodeJS, express, cors, prisma, nodemon)
Executar
	-npm start // ou
	-npx nodemon // ou
	-node server.js
